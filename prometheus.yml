global:
  scrape_interval: 5s # Как часто забирать метрики

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080'] # host.docker.internal позволяет контейнеру видеть ваш компьютер
