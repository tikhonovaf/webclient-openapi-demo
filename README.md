# Pet Store Aggregator (Spring Boot 3.4)

Это высокопроизводительный агрегатор данных, построенный на **Spring WebFlux**. Приложение собирает информацию о питомце и статусе заказа из двух независимых внешних сервисов (Pet Service и Store Service), используя неблокирующий **WebClient**.

---

## 🚀 Основные возможности

* **Reactive Stack**: Полностью неблокирующая обработка запросов на базе Project Reactor.
* **API-First Design**: Автоматическая генерация DTO и клиентов из спецификаций OpenAPI 3.0.
* **Resilience4j Integration**:
    * **Retry**: Автоматические повторы запросов при кратковременных сетевых сбоях.
    * **Circuit Breaker**: Защита от каскадных падений при отказе внешних систем.
    * **Fallback**: Возврат корректных дефолтных данных вместо необработанных ошибок.
* **Observability**: Расширенный мониторинг через Spring Boot Actuator и интеграция со Swagger UI.

---

## 🏗 Архитектура

Приложение вызывает внешние сервисы параллельно, что значительно сокращает время ответа по сравнению с классическим последовательным подходом.



1.  **Параллельность**: Использование `Mono.zip` для одновременного опроса Pet и Store API.
2.  **Изоляция**: Генерация клиентов в разные Java-пакеты для предотвращения конфликтов моделей.

---

## 🛠 Технологический стек

* **Java 17**
* **Spring Boot 3.4.1** (WebFlux, AOP, Actuator)
* **Maven** (openapi-generator-maven-plugin)
* **Resilience4j** (Circuit Breaker & Retry)
* **WireMock** (для интеграционного тестирования сценариев отказов)

---

## 📂 Структура проекта

* `src/main/resources/api/` — файлы спецификаций:
    * `pet_api.yaml` (Внешний сервис)
    * `store_api.yaml` (Внешний сервис)
    * `internal_api.yaml` (Наш API)
* `src/main/java/com/example/petstore/config/` — настройка WebClient и ApiClient.
* `src/main/java/com/example/petstore/service/` — бизнес-логика и стратегии устойчивости.
* `src/test/java/com/example/petstore/` — интеграционные тесты со сценариями WireMock.

---

## 🔨 Сборка и запуск

### 1. Генерация кода
Поскольку проект использует генерацию кода, перед первым запуском или после изменения `.yaml` файлов выполните:
```bash
mvn clean compile